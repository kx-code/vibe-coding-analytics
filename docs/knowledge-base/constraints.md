# Constraints

- Keep the runtime dependency-free.
- Do not overwrite existing project files.
- Keep generated files generic; project facts belong in the target repository.
- `evolve` must promote repeated work into durable harness assets, not just prose advice.
- The skill must mention `init`, `analytics`, `evolve`, and `/loop`.
