# Init Workflow

Initialize or refresh the minimum viable AI coding harness.

Run:

```bash
npx vibe-coding-analytics init --write
```

Review generated files, adapt commands to the project, then run validation.
Keep tool-specific adapters under their own directories only when that tool is
actually used by the project.
