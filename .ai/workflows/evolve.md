# Evolve Workflow

Improve the harness from recent repeated work.

Look for repeated failures, repeated user corrections, recurring commands,
review feedback, CI failures, and missing context. Promote each pattern into a
test, validator, rule, workflow, skill, reviewer, ADR, or scheduled check.

For optional CI mining:

```bash
npx vibe-coding-analytics evolve --ci-failures
```
