Initialize a minimal AI coding harness for this repository.

Run:

```bash
npx vibe-coding-analytics init --write
```

Then adapt generated files to the project, run available validation, and report remaining gaps.
